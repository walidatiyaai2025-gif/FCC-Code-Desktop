name: P06-007 Reconciliation Worker

on:
  push:
    branches:
      - worker/fccd-p06-007-reconcile

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: windows-2025
    timeout-minutes: 5
    steps:
      - name: Smoke
        shell: pwsh
        run: Write-Host 'P06-007 reconciliation worker trigger is valid.'
